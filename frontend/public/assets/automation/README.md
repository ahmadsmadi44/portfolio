# n8n walkthrough video

Place the finished portfolio walkthrough here as:

`n8n-workflow-walkthrough.mp4`

The homepage checks for that exact public path. Until the file exists, it shows the designed workflow preview and labels the walkthrough as upcoming. Once the MP4 is added, the same space becomes an inline video player without a code change.
